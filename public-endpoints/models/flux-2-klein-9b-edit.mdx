---
title: "Flux 2 Klein 9B Edit"
sidebarTitle: "Flux 2 Klein 9B"
description: "Multi-image editing model for combining elements from multiple source images."
---

Flux 2 Klein 9B Edit is a powerful multi-image editing model that combines elements from multiple source images based on text instructions. You can use it to place subjects from one image into another, merge visual elements, or create composite scenes.

<Card title="Try in playground" icon="play" href="https://console.runpod.io/hub/playground/image/flux-2-klein-9b-edit" horizontal>
  Test Flux 2 Klein 9B Edit in the Runpod Hub playground.
</Card>

| | |
|---|---|
| **Endpoint** | `https://api.runpod.ai/v2/flux-2-klein-9b-edit/runsync` |
| **Pricing** | \$0.03 per image |
| **Type** | Image editing |

## Request

All parameters are passed within the `input` object in the request body.

<ParamField body="input.prompt" type="string" required>
  Text instruction describing the edit, such as "Put the person in image1 into image2" or "Combine the background from image1 with the subject from image2".
</ParamField>

<ParamField body="input.images" type="array" required>
  Array of image URLs to edit or combine. Supports 1-5 images.
</ParamField>

<ParamField body="input.seed" type="integer" default="-1">
  Random seed for reproducibility. Set to -1 for random.
</ParamField>

<ParamField body="input.enable_base64_output" type="boolean" default="false">
  Return result as base64 string instead of URL.
</ParamField>

<RequestExample>
```bash cURL
curl -X POST "https://api.runpod.ai/v2/flux-2-klein-9b-edit/runsync" \
  -H "Authorization: Bearer $RUNPOD_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "input": {
      "prompt": "Put the person from image1 into the background of image2",
      "images": [
        "https://example.com/person.jpg",
        "https://example.com/background.jpg"
      ],
      "seed": -1
    }
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.runpod.ai/v2/flux-2-klein-9b-edit/runsync",
    headers={
        "Authorization": f"Bearer {RUNPOD_API_KEY}",
        "Content-Type": "application/json",
    },
    json={
        "input": {
            "prompt": "Put the person from image1 into the background of image2",
            "images": [
                "https://example.com/person.jpg",
                "https://example.com/background.jpg",
            ],
            "seed": -1,
        }
    },
)

result = response.json()
print(result["output"]["image_url"])
```

```javascript JavaScript
const response = await fetch(
  "https://api.runpod.ai/v2/flux-2-klein-9b-edit/runsync",
  {
    method: "POST",
    headers: {
      Authorization: `Bearer ${RUNPOD_API_KEY}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      input: {
        prompt: "Put the person from image1 into the background of image2",
        images: [
          "https://example.com/person.jpg",
          "https://example.com/background.jpg",
        ],
        seed: -1,
      },
    }),
  }
);

const result = await response.json();
console.log(result.output.image_url);
```
</RequestExample>

## Response

<ResponseField name="id" type="string">
  Unique identifier for the request.
</ResponseField>

<ResponseField name="status" type="string">
  Request status. Returns `COMPLETED` on success, `FAILED` on error.
</ResponseField>

<ResponseField name="delayTime" type="integer">
  Time in milliseconds the request spent in queue before processing began.
</ResponseField>

<ResponseField name="executionTime" type="integer">
  Time in milliseconds the model took to edit the images.
</ResponseField>

<ResponseField name="output" type="object">
  The generation result containing the image URL and cost.

  <ResponseField name="output.image_url" type="string">
    URL of the edited image. This URL expires after 7 days.
  </ResponseField>

  <ResponseField name="output.cost" type="float">
    Cost of the generation in USD.
  </ResponseField>
</ResponseField>

<ResponseExample>
```json 200
{
  "id": "sync-a1b2c3d4-e5f6-7890-abcd-ef1234567890-u1",
  "status": "COMPLETED",
  "delayTime": 18,
  "executionTime": 4521,
  "output": {
    "image_url": "https://image.runpod.ai/abc123/output.png",
    "cost": 0.03
  }
}
```

```json 400
{
  "id": "sync-a1b2c3d4-e5f6-7890-abcd-ef1234567890-u1",
  "status": "FAILED",
  "error": "Invalid image URL in images array"
}
```
</ResponseExample>

<Warning>
Image URLs expire after 7 days. Download and store edited images immediately if you need to keep them.
</Warning>

## Cost calculation

Flux 2 Klein 9B Edit charges \$0.03 per image generated.
