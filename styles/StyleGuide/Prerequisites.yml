# Rung: Stated, unconditional
# Source: style/client_style_guide.md ("Requirements (rather than 'prerequisites')")
#         and PROMPTLESS.md ("Requirements (rather than \"prerequisites\")"). No hedge.
extends: existence
message: "Use a 'Requirements' heading instead of '%s' (Runpod style guide)."
link: "https://docs.runpod.io"
level: error
scope: heading
ignorecase: true
tokens:
  - 'Prerequisites?'
