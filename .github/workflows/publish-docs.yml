# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-12-21T18:43:39.465Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: Publish 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Generate branch version string
        run: |
          BRANCH_NAME="${{ github.head_ref || github.ref || github.event.ref }}"
          BRANCH_VERSION=$(echo $BRANCH_NAME | tr -cd '0-9.')
          echo "BRANCH_VERSION=$BRANCH_VERSION" >> $GITHUB_ENV

      - name: Run `docs` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: docs v1.0 --key=${{ secrets.README_API_KEY }}
